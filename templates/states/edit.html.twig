{% extends 'base.html.twig' %}

{% block title %}Edit States{% endblock %}

{% block body %}
    <h1>Edit States</h1>

    {{ include('states/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('states_index') }}">back to list</a>

    {{ include('states/_delete_form.html.twig') }}
{% endblock %}
